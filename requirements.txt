# Core
torch>=2.5.1
numpy>=1.26.4

# Allowed extras (3)
matplotlib>=3.9.1       # all plots and visualisations
einops>=0.8.0           # tensor rearrangement in POCO attention layers
scipy>=1.15.2           # calibration, statistics, signal processing

# Optional — only needed to run preprocess.py from raw HDF5 files
# These are NOT required if data/processed/*.npz already exists
# pip install h5py scikit-learn
